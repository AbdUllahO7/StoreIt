import { Models } from 'node-appwrite'
import React from 'react'

const Card = ({file} : {file : Models.Document}) => {
    return (
        <div>
            
        </div>
    )
}

export default Card
